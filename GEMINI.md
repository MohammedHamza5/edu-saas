# GEMINI.md — دليل الذكاء الاصطناعي والقواعد الهندسية المعتمدة
## منصة SaaS التعليمية White-Label (V1)

> هذا الملف هو المرجع الأساسي المباشر لوكيل الذكاء الاصطناعي (Antigravity AI Agent). يحدد القواعد الدستورية، فلسفة التصميم، والمنطق الهندسي خلف كل اختيار معماري، لضمان استمرارية التفكير الموحد وعدم الانحراف عن الخطة المعتمدة.

---

## 1. فلسفة الاختيارات المعمارية: "لماذا كذا وليس كذا؟"

بناء الأنظمة الموجهة للإنتاج (Production SaaS) لا يعتمد على تجربة أحدث صيحة (Hype)، بل على تقليل المخاطر وسرعة الإطلاق والاستقرار الأمني. إليك التحليل الهندسي للقرارات:

### أ) إدارة الحالة: Cubit (`flutter_bloc`) وليس `Riverpod`
- **لماذا Cubit؟**
  1. طبيعة شاشات V1 (قوائم، تفاصيل، نماذج) تدور حول 4 حالات واضحة: `Initial`, `Loading`, `Loaded`, `Error`.
  2. توفير نمط متسق وصارم للـ MVVM يسهل اختباره ومراجعته، مع استهلاك ذاكرة منخفض وفصل تام بين منطق الأعمال والواجهة.
- **لماذا لم نختر Riverpod؟**
  Riverpod ممتاز ولكنه يقدم مرونة مفرطة في ربط الـ Providers عالميًا مما قد يغري بتشتيت المنطق، وفريق العمل يملك خبرة إنتاجية أعمق وأسرع مع Bloc/Cubit.

### ب) الـ Backend: `Supabase` + `Edge Functions` وليس `Custom Node.js/Express VPS`
- **لماذا Supabase؟**
  1. يقدم نظام Postgres علائقي كامل ومثبت.
  2. تقنية **Row Level Security (RLS)** تسمح بعزل الـ Multi-Tenancy على مستوى محرك قاعدة البيانات نفسه، مما يلغي خطر تسرب البيانات بين المدرسين حتى لو حدث خطأ برمجي في التطبيق.
  3. يدير Auth و Storage و Realtime في مكان واحد بدون الحاجة لإدارة خوادم (Zero DevOps Overhead).
- **لماذا Edge Functions؟**
  العمليات الحساسة (مثل اعتماد طالب، بدء امتحان، تسليم امتحان وحساب الدرجة) يجب أن تكون **Atomic** وغير خاضعة لتحكم العميل (Flutter Client is Untrusted).

### ج) معالجة الفيديو: `Bunny Stream` وليس `Supabase Storage`
- **لماذا Bunny Stream؟**
  1. استضافة وبث الفيديو عبر تخزين الكائنات العادي (Object Storage) مكلفة جدًا في الباندويث (Egress Costs) وتفتقر للتحويل التلقائي لجودات متعددة (Transcoding: 1080p, 720p, 480p, HLS).
  2. يوفر حماية قوية للروابط عبر الرموز الزمنية (Tokenized Signed URLs)، مما يمنع تنزيل الفيديو بسهولة أو مشاركته.
  3. قاعدة البيانات تحفظ فقط المعرفات والبيانات الوصفية (`video_id`, `duration`, `status`) مما يبقي قاعدة البيانات خفيفة وسريعة.

### د) تخزين التوكنات: `flutter_secure_storage` وليس `shared_preferences`
- **لماذا التخزين الآمن؟**
  - `shared_preferences` يحفظ البيانات كنص عادي (Plain Text) على ملف XML/Plist غير مشفر في الهاتف.
  - `flutter_secure_storage` يستخدم Keychain في iOS و KeyStore المشفر بـ AES في Android لحماية جلسات الدخول والتوكنات.

### هـ) التوجيه: `go_router` Declarative
- **لماذا go_router؟**
  1. دعم الويب وعناوين URL وروابط التوجيه العميق (Deep Linking) بسلاسة.
  2. إمكانية تقسيم التطبيق إلى Shell Routes منفصلة ومحمية بالكامل لكل دور (`TeacherShell`, `StudentShell`, `ParentShell`) بدلاً من التحقق اليدوي في كل شاشة.

### و) التصميم: الهوية الرياضية الأكاديمية (Modern Mathematical Academic SaaS)
- **الهوية البصرية:** ليست واجهة أطفال كرتونية وليست لوحة تحكم شركات باردة. استخدام شبكات دقيقة، رموز رياضية هندسية خافتة، وأرقام وإحصائيات بارزة كعناصر بصرية.
- **Tokens فقط:** ممنوع نهائيًا استخدام ألوان مباشرة `Color(0xFF...)` أو أحجام عشوائية داخل الـ Widgets. كل شيء يرجع لنظام الـ Theme الموحد.

### ز) نموذج العمل دون إنترنت: Online-First + Offline-Tolerant وليس Offline-First/Drift
- **لماذا لم نستخدم Drift أو قاعدة بيانات محلية معقدة؟**
  نظام الامتحانات وتصحيحها ورصد الحضور والموافقات يعتمد على الحقيقة المركزية اللحظية (Single Source of Truth). بناء محرك مزامنة كامل (Sync Engine) لحل التعارضات يستنزف وقت التطوير ويجلب مخاطر في نزاهة الامتحانات. النظام يتحمل انقطاع النت المؤقت دون أن ينهار.

### ح) حقن التبعيات (DI): Pure Constructor Injection + Composition Root وليس `get_it`
- **لماذا Pure Constructor Injection؟**
  1. **الالتزام الأقصى بمبادئ SOLID و Inversion of Control:** الكلاس يعلن صراحة وبشفافية تامة عن اعتماداته عبر المنشئ (`AuthCubit({required AuthRepository repository})`) دون إخفاء التبعيات.
  2. **اختبارات وحدة نقية (Pure Unit Testing):** كل كلاس يمكن اختباره بمفرده بتمرير Mock Repository مباشرة في المنشئ دون الحاجة لتهيئة حاوية عامة أو مسح حالتها بعد كل اختبار (`sl.reset()`).
  3. **صفر حزم خارجية:** لا يتطلب باكدجات إضافية (`get_it` / `injectable`)، مما يبقي الـ Dependencies خفيفة ومتوافقة 100% مع الستاك المقفول للمشروع.
- **لماذا لم نختر `get_it`؟**
  الـ Service Locator يعتبر نمطاً مضاداً (Anti-Pattern) لأنه يخفي التبعيات داخل حاوية عامة ويجعل الكود معتمداً على مسجل عالمي (Global State) بدلاً من حقن التبعيات الصريح.

---

## 2. الثوابت الدستورية الحاكمة (Non-Negotiable Guardrails)

1. **Flutter عميل غير موثوق به أبدًا:**
   أي قيمة لـ `tenant_id`, `role`, `status`, `score`, `is_correct` قادمة من Flutter = صفر ثقة. الحقيقة تُستخرج دائماً من `auth.uid()` → `public.users`.
2. **RLS هو خط الدفاع الحقيقي:**
   الصلاحية غير المفروضة على مستوى الـ Database غير موجودة أمنيًا. إخفاء زر في الواجهة ليس حماية.
3. **العزل التام بين الـ Tenants:**
   لا يوجد استثناء "مؤقت" أثناء التطوير.
4. **البيانات الأكاديمية غير قابلة للتعديل بأثر رجعي (Immutability):**
   أي امتحان نُشر (Published) يتحول لـ Snapshot مجمد. أي تعديل ينشئ نسخة جديدة (`exam_versions`).
5. **العمليات الحساسة عبر Edge Functions:**
   لا يتم اعتماد طالب، أو حساب درجة امتحان، أو تفويض تشغيل فيديو داخل كود العميل.
6. **الحالات الأربع الإلزامية لكل واجهة:**
   - Loading (Skeleton)
   - Empty
   - Error
   - Success
7. **RTL / LTR والتعريب الإلزامي:**
   ممنوع أي نص ثابت مكتوب باليد داخل الـ Widgets. كل النصوص تُقرأ من ملفات ARB (`app_ar.arb` / `app_en.arb`).
8. **الأمان الصارم للبيانات السرية (Secrets):**
   لا Service Role Key ولا API Secrets في Flutter أو Git نهائيًا.

---

## 3. خريطة الملفات المرجعية في المشروع

| المسار | المحتوى والهدف |
|---|---|
| `AGENTS.md` | عقد التشغيل الذاتي وقواعد التنفيذ المعتمدة |
| `docs/canonical/01_AI_CONTEXT.md` | ملخص السياق المعماري السريع |
| `docs/canonical/02_PRD.md` | وثيقة متطلبات المنتج والنطاق |
| `docs/canonical/03_ARCHITECTURE_MASTER.md` | الوثيقة المعمارية الأم الشاملة (مصدر الحقيقة الأعلى) |
| `docs/canonical/04_DATA_MODEL.md` | الـ 24 جدولاً، الفهارس، وقواعد الأعمال |
| `docs/canonical/05_API_CONTRACT.md` | عقود الـ Edge Functions والأخطاء |
| `docs/canonical/06_UI_UX_SCREENS.md` | الـ 57 شاشة والرحلات الحرجة |
| `docs/canonical/07_ROADMAP_TASKS.md` | خارطة الطريق والمهام الـ 16 المرتبة |
| `docs/canonical/08_AGENTS_RULES.md` | قواعد وسلوك الوكيل البرمجي |
| `docs/canonical/09_DEFINITION_OF_DONE.md` | معايير الجودة والـ DoD الإلزامية |
| `docs/canonical/10_DECISIONS.md` | سجل القرارات والممنوعات |
| `supabase/migrations/0001_init.sql` | المخطط الكامل لقاعدة البيانات وسياسات RLS |
